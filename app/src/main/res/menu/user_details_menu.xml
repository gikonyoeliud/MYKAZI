<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Edit account (visible only for self) -->
    <item
        android:id="@+id/action_edit_account"
        android:title="Edit Account"
        app:showAsAction="never" />

    <!-- Delete account (visible only for self) -->
    <item
        android:id="@+id/action_delete_account"
        android:title="Delete Account"
        app:showAsAction="never" />

    <!-- Block user (visible only for other users) -->
    <item
        android:id="@+id/action_block_user"
        android:title="Block User"
        app:showAsAction="never" />

    <!-- Unblock user (visible only for other users) -->
    <item
        android:id="@+id/action_unblock_user"
        android:title="Unblock User"
        app:showAsAction="never" />

    <!-- Sign out (always visible) -->
    <item
        android:id="@+id/action_sign_out"
        android:title="Sign Out"
        app:showAsAction="never" />

</menu>
